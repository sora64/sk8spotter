<section class="container">
  <h3 class="skatepark-name">{{skatepark.name}}</h3>
  <section class="flex-row justify-center">
    {{#each skatepark.images as |image|}}
    {{> image-details}}
    {{/each}}
  </section>
</section>
