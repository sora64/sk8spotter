{{!-- <a href="/pic/{{id}}">
  <article class="skater skater-1">
      <img src="/images/process_images/{{filename}}" alt="{{description}}">

      <div class="skater-bio text-left">
        <h3>{{title}}</h3>
        <!-- <h4>Speed / Strength</h4> -->
        <p>{{description}}</p>
      </div>
  </article>
</a> --}}

<a href="/pic/{{id}}" class="grid-container fluid">
  <div class="grid-y grid-padding-y grid-margin-y align-center text-center">
    <section class="cell small-12 flex-container flex-dir-column large-flex-dir-row">
      <div class="callout flex-child-auto media-object align-center image-container">
        <div class="thumbnail holder">
          <img
            src="/images/process_images/{{filename}}"
            alt="{{description}}"
          />
          <div>
            <h3 class="flavor-text">{{title}}</h3>
            <p class="image-text flavor-text">{{description}}</p>
          </div>
        </div>
      </div>
    </section>
  </div>
</a>