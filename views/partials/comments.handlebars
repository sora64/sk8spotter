<div class=''>
  {{#each this}}
    <section class=''>
      <div class='meta'>
        {{user.username}}
        on
        {{format_date created_at}}
        at
        {{format_time created_at}}
      </div>
      <div class=''>
        {{comment_text}}
      </div>
    </section>
  {{/each}}
</div>